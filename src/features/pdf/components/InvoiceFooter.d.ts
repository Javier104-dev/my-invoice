type Props = {
  companyName: string;
  closingMessage: string;
};
declare const InvoiceFooter: ({
  companyName,
  closingMessage,
}: Props) => import('react/jsx-runtime').JSX.Element;
export default InvoiceFooter;
